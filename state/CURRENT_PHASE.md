# CURRENT LEARNING PHASE

- **Current Phase:** PHASE 3 — Git, Bash Scripting & Python cho DevOps
- **Current Status:** Hoàn thành Buổi 11 (Bash Scripting Fundamentals, Variables, Conditionals, Arguments & Exit Codes). Chuẩn bị Buổi 12 (Bash Advanced Automation: Functions, Arrays, Loops `for/while`, Log Parser & Automated Backup Script).
- **Current Week:** Tuần 3
- **Completed Outputs:**
  1. Nắm vững bản chất Shebang `#!/bin/bash`, Biến (Variables), Quy tắc Quoting (`""` vs `''`) và Tham số ($1, $2, $#).
  2. Viết Pseudocode chuẩn logic lập trình trước khi viết mã nguồn Bash.
  3. Viết script `check_endpoint.sh` tự động kiểm tra Health Check Endpoint bằng `curl` và `http_code`.
  4. Kiểm soát mã thoát Exit Code ($?): Phân biệt Exit code `0` (Success) và khác `0` (Failure).
  5. Thực hành sự cố Failure Injection 11.2: Sử dụng cờ `set -e` để chặn đứng trôi lỗi trong Bash script cho Pipeline CI/CD.
  6. Commit và push bài lab `lab-11-bash` lên GitHub `Minhlike/devops-learning`.
